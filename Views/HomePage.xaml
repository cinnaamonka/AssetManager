<Page x:Class="AssetManager.Views.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AssetManager.Views"
      xmlns:vm="clr-namespace:AssetManager.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePage">

    <Page.DataContext>
        <vm:HomePageVM />
    </Page.DataContext>

    <Grid Margin="20">
        <!-- Header with Welcome Text -->
        <StackPanel Orientation="Vertical" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="0,0,0,20">
            <TextBlock Text="Welcome to Project Manager" FontSize="32" FontWeight="Bold" Foreground="#333333" Margin="0,0,0,10" HorizontalAlignment="Left"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Projects" FontSize="24" FontWeight="Bold" Foreground="#333333" VerticalAlignment="Center"/>

                <!-- Search Box -->
                <TextBox Width="200" Height="30" Margin="20,0,0,0" VerticalAlignment="Center"
                         HorizontalAlignment="Left"/>

                <!-- Add Project Button -->
                <Button Content="Add Project" Width="100" Height="30" Margin="20,0,0,0" VerticalAlignment="Center"
                        Background="#4A90E2" Foreground="White" FontWeight="Bold"/>
            </StackPanel>
        </StackPanel>

        <!-- Table -->
        <DataGrid Name="ProjectsDataGrid" AutoGenerateColumns="False" Margin="0,120,0,0"
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  Background="White" Foreground="#333333" BorderBrush="Gray" BorderThickness="1"
                  RowBackground="White" AlternatingRowBackground="#F8F8F8">

            <!-- Columns -->
            <DataGrid.Columns>
                <DataGridTextColumn Header="S.No" Binding="{Binding SerialNumber}" Width="50"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="From" Binding="{Binding From}" Width="150"/>
                <DataGridTextColumn Header="To" Binding="{Binding To}" Width="150"/>
                <DataGridTextColumn Header="Days" Binding="{Binding Days}" Width="50"/>
                <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="200"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>